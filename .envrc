watch_file flake.nix
watch_file flake.lock
watch_file outputs/devShells.nix

if git rev-list HEAD..@{u} --count 2>/dev/null | grep -q '^[1-9]'; then
    echo "--- Skip Devshell: Flake is behind remote ---"
    return
fi

use flake
