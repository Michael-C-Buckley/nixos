apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    service:
      spec:
        externalIPs:
          - 100.64.128.82  # Netbird interface
    tlsStore:
      default:
        defaultCertificate:
          secretName: wildcard-groovyreserve-com
